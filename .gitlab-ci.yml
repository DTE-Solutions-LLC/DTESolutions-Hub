image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html pulse-breakdown.html public/
    - cp -r assets public/
    - cp -r legal public/
    - cp -r src public/ || true
  artifacts:
    paths:
      - public
  only:
    - main
